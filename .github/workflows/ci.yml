name: CI — video-script-extractor

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Bash syntax check
        shell: bash
        run: bash -n extract-video-script.sh
